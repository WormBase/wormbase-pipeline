# Usage of the Laboratory 'CB' tag in ~wormpub/analysis/scripts 
# 000609

# CDS_to_LAB:if ($from_lab eq "CB") {
# EMBLSubmit:Query Find Genome_Sequence From_Laboratory = CB AND Finished AND DNA
# aceprep1_4:From_Laboratory CB
? cdna2ace:  echo "Location CB"
# check_DNA.pl:#    $count = $db->fetch(-query=> 'find Genome_sequence where From_Laboratory = CB AND Finished');
# make_wormpep.pl:    if ($lab eq "CB") {$lab = "CAMBRIDGE";}
# makelinks:    print "From_laboratory CB\n" ;
# makelinks:    print "From_laboratory CB\n" ;
# patch_script:#    $count = $db->fetch(-query=> 'find Genome_sequence where From_Laboratory = CB AND Finished');
# unfinished-aceprep:    print ACE "From_laboratory CB\n" ;
